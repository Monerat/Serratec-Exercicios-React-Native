import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    badge:{
        alignItems: "center",
        paddingHorizontal: "5%",
        paddingVertical: 3,
        backgroundColor: "#5C5FB2",
        borderRadius: 30,
     }
})